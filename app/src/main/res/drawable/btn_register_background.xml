<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#6200EE" />  <!-- Color de fondo (puedes cambiarlo) -->
    <corners android:radius="50dp" />  <!-- Bordes redondeados -->
    <size android:height="48dp" /> <!-- Tamaño del botón -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="12dp"
        android:bottom="12dp" />
</shape>
